using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Repository : MonoBehaviour
{
    //we need a dictionary to hold baby cheeses!
    public static Dictionary<string, List<Transform>> repositoryDictionary = new Dictionary<string, List<Transform>>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
